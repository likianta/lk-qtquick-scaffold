import QtQuick

Window {
    visible: true
    width: 400
    height: 300
    color: 'white'  // change to 'yellow', 'red', etc. then reload.

    Text {
        anchors.centerIn: parent
        text: 'Hello world!'
    }
}
